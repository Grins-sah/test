#include<stdio.h>
#include<stdlib.h>
int main(){
    printf("%n");
    /**
        %d or %i: Used to print a signed or decimal integer
        %c: Used to print a single character
        %s: Used to print a string
        %f: Used to print a floating-point number with six digits after the decimal
        %lf: Used to print a double-precision floating-point number
        %x or %X: Used to print a hexadecimal number
        %o: Used to print an octal number
        %p: Used to print a pointer's value (memory address)
        %u: Used to print an unsigned integer
        %%: Used to print a percent sign
        %E: Used for scientific notation
        %hi: Used for a signed short integer
        %hu: Used for an unsigned short integer
        %l or %ld or %li: Used for long
        %Lf:Used for a long double
        %lu: Used for an unsigned int or unsigned long
        %lli or %lld: Used for long long
        %llu: Used for an unsigned long long
        %n: Prints nothing 
    */
}